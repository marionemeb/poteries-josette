# Template committé (valeurs non sensibles) — copier en .env pour développer :
#   cp .env.dist .env
# .env lui-même est gitignoré ; les vraies valeurs de prod vivent uniquement sur OVH.
APP_ENV=dev
APP_SECRET=changeme
DATABASE_URL=mysql://josette:josette@db:3306/josette_dev?serverVersion=8.0.46
MAILER_DSN=null://null
